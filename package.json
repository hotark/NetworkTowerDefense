{
  "name": "network-tower-defence",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "^6.2.0",
    "terser": "^5.37.0"
  },
  "dependencies": {
    "@preact/signals-core": "^1.8.0"
  }
}
